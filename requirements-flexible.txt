# Core Flask dependencies - required for the application to work
Flask>=2.3.0
Flask-SQLAlchemy>=3.0.0
Flask-JWT-Extended>=4.5.0
Flask-CORS>=4.0.0
Flask-Limiter>=3.5.0
Flask-WTF>=1.2.0
Werkzeug>=2.3.0

# Image processing and utilities
Pillow>=10.0.0
python-dotenv>=1.0.0
bcrypt>=4.0.0

# Optional development tools (can be skipped if causing conflicts)
gunicorn>=21.0.0
pytest>=7.4.0
pytest-flask>=1.3.0

# Code formatting tools (optional - only install if needed for development)
# black>=23.11.0
# flake8>=6.1.0